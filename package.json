{
  "name": "effect-types",
  "version": "0.0.1",
  "description": "",
  "private": false,
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsup src/**/*.ts --format cjs,esm --dts",
    "lint": "tsc",
    "dev": "vitest",
    "test": "vitest run",
    "ci": "pnpm run lint && pnpm run test && pnpm run build",
    "release": "pnpm run ci && changeset publish"
  },
  "author": "Jesse Kelly",
  "license": "MIT",
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "@effect/data": "^0.12.2",
    "@effect/schema": "^0.13.0",
    "fast-check": "^3.7.1",
    "husky": "^8.0.3",
    "tsup": "^6.6.3",
    "typescript": "^4.9.5",
    "vitest": "^0.30.1"
  },
  "peerDependencies": {
    "@effect/data": "^0.7.1",
    "@effect/schema": "^0.6.0"
  },
  "dependencies": {
    "AST": "link:@effect/schema/AST",
    "hotscript": "^1.0.11",
    "source-map": "^0.7.4"
  }
}